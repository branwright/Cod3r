<%= form_for(@recent) do |f| %>
  <% if @recent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recent.errors.count, "error") %> prohibited this recent from being saved:</h2>

      <ul>
      <% @recent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :input %><br />
    <%= f.text_area :input, :size => '40x5' %>
  </div>

  <div class="actions">
    <%= f.submit("enCod3/ deCod3") %>
  </div>

  <div class="field">
    <%= f.label :output %><br />
    <%= f.text_area :output, :size => '40x5' %>
  </div>

<% end %>
